# deps + provisioned binaries (re-create with: npm install --no-save ffmpeg-static ffprobe-static)
node_modules/
bin/
package-lock.json

# render output + QA scratch (final MP4 lives in ../../public/downloads/)
renders/
qa/

# keep: index.html, package.json, meta.json, hyperframes.json, AGENTS.md, CLAUDE.md, assets/fonts/*.woff2

# VO segment build intermediates (final = assets/vo.wav)
assets/seg/

# unused block reference art (we use vela-phone/vela-laptop.png)
models/hyperframes-*.png
